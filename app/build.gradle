plugins {
    id 'com.android.application'
}

android {
    namespace 'com.entropy.pmaec'
    compileSdk 34

    defaultConfig {
        applicationId "com.entropy.pmaec"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
}
